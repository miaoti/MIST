# Tenancy-window plan — 3-cold-review reconciliation (2026-07-10)

Reviews of `c2c3/tenancy-window-plan.md` rev 1: **A feasibility (live-verified) / B evidential /
C scope-value — ALL GO-WITH-CHANGES.** Every fix is plan text; folded as **rev 2** in place.
Per the user's directive, rev 2 goes BACK to the same three reviewers for a confirmation pass;
execution starts only on unanimous acceptance.

Live facts A established (bank them): Envoy→Jaeger tracing ALREADY wired mesh-wide (extensionProvider
`jaeger` → collector:4317, Telemetry 100% sampling, since 07-03); bookinfo NOT gone — all 6 deploys at
0/0 WITH the reviews→v3 VS pin + DRs + gateway intact; sock-shop 14 deploys at 0/0, July injection
topology preserved, gateway VS live; TT = 47 deploys + 3 sts (53 pods), no HPA/CronJob to fight
scale-0; agent jar on node; helm 3.21.1 present (no repos configured); Jaeger v2.14 otlp+jaeger+zipkin
receivers; RAM now 1 GiB free (Phase A mandatory first); SS Java legs = 500Mi limit + JAVA_OPTS
`-Xmx128m` + sleuth explicitly off.

## Converged dispositions (rev-2 pins)

| # | Finding | By | Rev-2 disposition |
|---|---|---|---|
| T1 | **Headline overstates**: the pair's only separating column (`mist_trace_shape`) stays Branch-B unrun; "FP/TP pair MEASURED" is false as written | C-B1, B-B2 | §0(1)+§6 rewritten: "the pair's COMPARATOR columns measured + traces banked; the MIST-side precision claim stays pre-registered pending the 2.5/E2 run". Close-out freeze amendment wording pre-registered (scoped like the N-vs-0 precedent row). Flip treatment pinned: `mist_trace_shape` cells overwrite to `not_applicable` (captured⇒as-deployed, Branch-B note) and the displaced R4 design targets (`no_flag` bookinfo / `flag` sockshop) are PRESERVED VERBATIM in the notes as the pre-registered claim |
| T2 | **No divergence rule; sockshop `naive=flag` is the LIKELY casualty** (rabbit@0 ⇒ connect fails before basicPublish ⇒ no producer span ⇒ no error span ⇒ `no_flag` = FN — a stronger both-directions-failure story, but not the pinned one) | B-B1 (+A mechanics concur) | §3 gains: (a) per-span-family CONTROL-LEG VALIDATION (HTTP server/client, AMQP producer, AMQP consume, Envoy client/server): family instrumented iff the control leg produces it; validated families ⇒ fault-leg cells MEASURED AS SCORED (no error span = `no_flag`, recorded); absent families ⇒ cells `not_applicable`/unfilled + instrumentation-gap disclosure. (b) Any measured verdict diverging from a pre-registered `oracle_expectation` ⇒ recorded as measured, expectation corrected with a dated refuted-by-capture note, pair narrative re-derived from measured cells only, and NO selector/scope/error-rule change after the first real capture |
| T3 | Teardown nested in optional B5 | C-B2 | Promoted to unconditional **B6** (scale down bookinfo+SS; de-instrument the 3 SS services) |
| T4 | **Scorer semantics under-pinned**: AMQP presence span is CONSUMER-kind not SERVER; Envoy error encoding differs (`http.status_code≥500`/response_flags); naive scope must be per-case; entry-op fragments; commit point ambiguous | A-M2, B-M3, B-m1, A-m9 | B3 rewritten as binding pins: presence kind = SERVER (HTTP deps) / **CONSUMER** (AMQP); error rule = `error=true ∨ otel.status_code=ERROR ∨ http.status_code≥500` (pinned NOW, mechanical); naive scope per-case service sets; entry fragments pinned (bookinfo `GET /productpage`, sockshop `POST /orders`); scorer extension COMMITTED before the first real capture; canary binding = serviceName/operation SPELLINGS + tag-encoding mappings ONLY, from control/throwaway traces only; kinds/dependency identity/exactly-one/verdict mapping/error-rule MEANING immutable; per-leg windowing restated (probe → fresh capture window → immediate export) |
| T5 | **Do NOT re-apply bookinfo samples** — live cluster already has reviews→v3 pinned; wholesale apply includes `virtual-service-all-v1.yaml` which would destroy it (v1 never calls ratings → both legs die) | A-M1, B-M2 (converged) | B1 rewritten: SCALE UP the existing 0/0 deployments; ASSERT (not apply) reviews→v3 VS + 4 DRs + gateway; apply nothing that exists; record the VS/DR manifests into the case replay preconditions |
| T6 | SS memory: 500Mi limit + JAVA_OPTS `-Xmx128m`; JTO **cannot** override a command-line -Xmx; agent adds ~80-150Mi native | A-M3, A-m7 | B2: bump container memory limit to 768Mi in the SAME patch (recorded in the sidecar attestation); heap untouched (agent memory is native/metaspace); **instrument at 0 replicas then scale up** (one boot round, flap-friendlier); canary OOME grep stays the tripwire |
| T7 | D1 pre-check one item short; two §2.3 riders dropped | A-M4, C-M3(b) | D1 adds: accounting-**Postgres** renders (or values-enable); frontend **graceful-ad** rider restored (doubles as the T13 benign capture); **flagd list re-freeze** vs pinned tag restored (~30 min at D2) |
| T8 | mesh-503 rider dropped from C2 | C-M3(a) | Restored with honest gating: "mesh-503 ack-verify CONDITIONAL on the plain-VS probe intercepting; else deferred-with-disclosure to 3a" |
| T9 | sql-probe convention gap: OTel case has NO runnable MIST column this wave | B-M4, C-m9 | Pinned: `readback.mist_bindable=false ⇒ mist_readback_oracle=not_applicable` (APPLICABILITY-BOUNDARY note, distinct from none-durable; provisional pending the 2.75 enablement decision — single-homed there); freeze §6 convention row at close-out; §0 wording discloses the flagship is comparator-measurement + boundary-documentation until MIST runs |
| T10 | Corpus arithmetic 19-21 unsupported; count = **17-18** | B-M1, C-m6 | Enumerated: +2 TeaStore, +2 OTel-Demo = 17; sockshop control leg = standalone case (+1 = 18; `trace_visibility` = twin's regime value per B-M3 pin); bookinfo control stays a leg (S2 precedent has no control case). Reporting line pre-stated: **8 pos / 10 neg** (with T13: +2 neg ⇒ 8/12, 20 cases). §1 corrected |
| T11 | Next tenancy flip unnamed; §2.6(b) nacos=1 stale | C-M4, A-m5 | §1 gains: "Default: OTel-Demo stays up; next window = OTel 2.75 enablement + its 3a S1 quota, then flip to TT revival for F-corpus/M-yield — user confirms at Phase E." Phase-A snapshot BEATS §2.6(b)'s stale `nacos=1` (nacos revives at 2/2 per the snapshot + joint-restart rule) |
| T12 | Sockshop case precondition text contradicted by the front-end descope | B-M5 | Pre-registered case-note amendment at flip: precondition revised to {orders/shipping/queue-master javaagents; entry = orders server span; front-end descoped + rationale}, dated, citing this plan |
| T13 | Add the bounded benign-capture rider (raters' binding shortage = captured S2: 3 vs ≥20) | C-M5 | New rider (≤0.5 day total, skip-on-pressure, AFTER each SUT's committed captures): TeaStore recommender-cold-start benign + OTel-Demo graceful-ad benign as S2 cases (by-docs, doc_citation = the rider verification itself) |
| T14 | If the window collapses, D outranks C | C | §6 line added: "time collapse ⇒ B then D; C is the deferrable phase" |
| T15 | Minors | all | TeaStore trace CELLS explicitly `not_applicable` (B-m2); TeaStore fresh user CREATED BEFORE DB-down (B-m3); presence=flag cells conditional on the control-leg baseline + unlinked-consume (separate-trace) counts as NOT claimable absence under broken propagation — applied to sockshop too (B-m4, A-m6); C2 refutation ⇒ capture NEITHER TeaStore leg (no dangling twin, R9 precedent; C-m8); C-M4 probe time-boxed: plain-VS check only ≤2 h, a miss IS the datum, EnvoyFilter authoring = 3a (C-m10); `helm repo add open-telemetry` or pinned tarball fetch (A-m8); boot-time OTLP retry noise expected at canary, not an export failure (A-m10) |

Budget re-pinned: **4-5 days** (A+C concur); Phase B alone remains the acceptable minimum under the
T1-scoped claim. Verdict: fold → rev 2 → confirmation pass with the same three reviewers → execute
on unanimous ACCEPT.
