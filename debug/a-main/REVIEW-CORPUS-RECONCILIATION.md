# C2 corpus rev-2 — 3-cold-review reconciliation (2026-07-09)

Folds the three independent blind reviews of the 11-case rev-2 corpus (commit `d062e52`):
- **A — oracle soundness**: ACCEPT-WITH-FIXES; 4 MAJOR + 7 MINOR; `body_marker`/`status`/`schema` correct in all 11.
- **B — migration faithfulness**: ACCEPT-WITH-FIXES; all findings MINOR; **nothing dishonest** (git-diffed all 6 legacy cases, read all 9 sidecars, checked every pin).
- **C — benchmark-design (hostile PC)**: ACCEPT-WITH-FIXES *as an explicitly-scoped SEED/pilot*; **REJECT if published as "the benchmark"**; 5 MAJOR + 3 MINOR + nitpicks.

## Through-line (the decision that organizes every fix)
C's dominant message, which A corroborates on the technical side: **this is a PILOT/SEED, not "the benchmark."** Only **2** captured *discriminating* positives exist (both fabricated-ack, both on `ts-inside-payment-service`), the FP/TP precision pair + breadth positives + entire S3 stratum are unrun, and 3 "S1" cases are actually clean controls. **Disposition: reposition the artifact honestly as a seed corpus + pre-registered scale plan, and enforce that honesty *inside* the files** — never present a `specified` case's `oracle_expectation` as a measured result; report "6 positives / 5 negatives", not "9 S1". Every MAJOR below is fixable by field edits + a freeze/schema reconciliation + disclosures; the new captures are the *scale plan* (deferred, need live deploys), not blockers to an honest pilot.

---

## Convergence map

| # | Finding | Raised by | Sev | Disposition |
|---|---|---|---|---|
| R1 | **Freeze↔schema disagree on S1**; 3–4 cases are S1+negative or S1+natural, violating freeze "S1⇒genuine+by-injection". "9 S1" reads as 9 positives but is 6 pos + 3 controls. | A-M4, C-F5, B(noted) | MAJOR | **AMEND freeze §2** to match the schema (schema already says "1 = positive by construction (injected/replicated/vendor/**natural**)"): S1 = positive-by-construction **OR** its paired clean control; `provenance ∈ {by-injection, natural, replicated, vendor}`; `negative_control` required on **positives only**. Always report **6 pos / 5 neg** (3 S1 controls + 2 S2 traps), never a bare "S1 count". |
| R2 | **oracle_expectation trace-column convention split**: captured-uninstrumented cases mix `no_flag` (adminroute-control, contacts-dedupe) and `not_applicable` (cancel/createaccount family). | A-M1, B(f3) | MAJOR | Adopt **capture_status-keyed evaluability** (state in freeze §4): **captured ⇒ as-deployed** (uninstrumented trace oracle ⇒ `not_applicable`); **specified ⇒ design-expectation** (precondition-gated, **never tallied as a result**). Fix adminroute-control + contacts-dedupe trace cols `no_flag`→`not_applicable`; fix adminroute-control's self-contradicting "span IS present … spans not recorded" note. |
| R3 | **fabricated-ack `trace_visibility=trace-invisible-by-construction` contradicts its all-`not_applicable` trace oracles**, and can't feed the freeze's promised N-vs-0 recall row (needs run-and-miss = `no_flag`, not `not_applicable`). Both run on the same uninstrumented deploy as their twins. | A-M2, C-F6/F8 | MAJOR | Per R2 (honest-as-captured): set `trace_visibility=trace-uninstrumented` on cancel-fabricatedack + createaccount-agreement; move the by-construction-invisibility to **prose as a pre-registered claim** requiring a future *traced* capture to earn the N-vs-0 row. |
| R4 | **bookinfo/sockshop none-durable pair: precision advantage unestablished.** Structural "error-span-under-2xx" FPs on both; `tracetest_presence` outcome depends on *which* span is asserted; the note's "ratings span IS present" is **factually wrong** (ratings scaled-to-zero ⇒ server span **absent**). MIST's `mist_readback=not_applicable` on both ⇒ `mist_trace_shape` is MIST's only oracle here and its edge isn't earned. | A-M3, C-F3 | MAJOR | **Name the semantic discriminator** in both cases: *entry-response-reflects-failure* (bookinfo renders "Ratings unavailable") vs *identical-to-success* (sockshop truly masked). Correct the span-presence claim. Add queue-master **consume-span** to sockshop's precondition (or downgrade its trace cols to `not_applicable` as-deployed). Mark both **SPECIFIED design-targets, not results**; report **all** comparators on the pair (incl. `tracetest_presence`), not just naive-vs-shape. |
| R5 | **No `contract_invariant` column ⇒ the "agreement/fairness" anchor is unscorable** and createaccount-agreement's 7-col row is identical to a MIST-only win, contradicting its own prose ("a state-clause comparator ALSO catches it"). | C-F2, A-m5 | MAJOR | **Relocate, don't add a column.** A contract comparator's verdict is *not* deterministic-by-construction (it depends on authoring/bindability) — that's the **g3 head-to-head's measured quantity** (`g3-comparator-tt/`, Rider-2 bindability), not a C2 baseline. Reframe C2's createaccount-agreement as a **body-carrying-CRUD read-back positive** (breadth: read-back catches POST-create lost writes, not just the cancel GET); cross-reference the head-to-head for the agreement role; **disclose the C2 table cannot distinguish a MIST-only win from an agreement.** Remove the scored-sounding claim. |
| R6 | **Construct bias**: captured discriminating positives are all synthetic fabricated-ack fork flags (fault *shaped* to be read-back-only-visible); benign traps each fool exactly one baseline while sparing MIST; **no benign case stresses the read-back oracle** (a success-shaped-clean 2xx that *legitimately* doesn't persist — idempotent/no-op). | C-F4 | MAJOR | **Now**: caveat fabricated-ack as a *synthetic worst-case exemplar* in each such case + the index; commit to reporting per-oracle FP **including MIST's read-back**. **Scale plan (deferred capture)**: add an idempotent-no-op benign control (TT lacks one by its own admission — borrow from another SUT); capture the natural positives. |
| R7 | **Pilot framing not enforced in the artifacts**; stale README describes only the 4 old seed cases; §5 counts SUTs with zero cases. | C-F1, C-nits | MAJOR | Stamp the corpus **SEED/pilot** (index header + every `specified` case); refresh the corpus README/manifest to cover all 11 cases + the scale plan; stop citing TeaStore/OTel/Boutique sites as delivered. |

## Singles (MINOR — field edits)

| # | Finding | By | Fix |
|---|---|---|---|
| R8 | adminroute twins disagree on `target.dependency.kind` (lostwrite=`downstream_service`, control=`persistence`) | A-m1, B-f1 | Both → `downstream_service`. |
| R9 | adminbasic `negative_control.present=true` but `replay_script=null`, no twin | A-m2, C-F7 | Mark explicitly deferred (capture_status=`specified`) in the block's script + note; keep `present=true` with a "deferred" marker, or set `present=false`. → set `present=false` + note (honest: the control is not yet authored). |
| R10 | sockshop `dependency{service:"shipping", kind:"broker"}` conflates service & broker | A-m3 | `service:"rabbitmq (shipping→queue-master AMQP leg)"`, keep `kind:"broker"`. |
| R11 | none-durable read-path cases carry nominal `expect_without_fault=landed`/`write_shape=whole` | A-m4 | Keep (enum has no no-write value) but flag **nominal** in the locator/prose (bookinfo already partly; make explicit). |
| R12 | bookinfo `ack_content_visibility=success-shaped-clean` arguable (page shows "unavailable") | A-m6 | Add one-line justification: human-readable HTML prose, not a machine sentinel/status field; a substring body oracle *could* FP → that's the R4 discriminator, disclosed. |
| R13 | "natural" slug on cancel-refund-natural is a misnomer (`injection_method=sut_injector`) | A-m7, B-a, C-nit | Keep the id (stable handle); clarify in prose: "natural" = the SUT's **genuine error-swallow ACK path** (`{1,"error"}`), reproduced via `drawbackFaultMode=fail` (a faithful toggle of the natural drawback-abort path); the g3 live mesh-sever produced the same ack. |
| R14 | orphan sidecars `tt-elig-genuine`/`tt-elig-benign` linked by no case | B-b, C-nit | Expected — they are the **§11 screening instrument**, not measurement cases. Note in the README so they're not read as dropped cases. |
| R15 | createaccount read-back is a global `GET /account` (all users) — permissive admin read-back | C-nit | Disclose the assumption (MIST has an admin/collection read-back endpoint) in the case + as a read-back-oracle applicability note. |
| R16 | contacts-dedupe sidecar internal `case_id:"tt-s2-contacts-dedupe"` ≠ dir name | B-f4 | Cosmetic; file-path link is correct. **Leave** (do not rewrite a captured artifact post-hoc; note the alias in the case). |

## What is DEFERRED to the pre-registered scale plan (needs live deploys — NOT blockers)
Traced captures to earn the N-vs-0 recall row (R3); the FP/TP pair live capture + queue-master consume instrumentation (R4); fork-built breadth positives adminroute/adminbasic (R6); an idempotent-no-op benign read-back control (R6); the S3 wild stratum. These are **pre-registered targets**, marked `specified`, and are never counted as measured results until captured.

## Execution order
1. This doc. 2. Freeze amendment (R1, R2 convention, §6 log). 3. Case field/prose edits (R2–R16). 4. Corpus README/manifest refresh + SEED stamp (R7) + scale plan. 5. FILE_INDEX + checklist sync. 6. Re-validate all 11. 7. Commit on `main_track` (no Co-Authored-By).
