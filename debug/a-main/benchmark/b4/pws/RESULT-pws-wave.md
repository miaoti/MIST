# RESULT — Wave PRE-WRITE STRENGTHENING (PWS) — consolidated wave status (2026-07-17)

**Plan:** `wave-prewrite-strengthening-plan.md` rev 2.2 (CONFIRMED 3/3). **User gate:** NO
write-up until ALL experiments incl. the rater study are done. This RESULT consolidates the
executable legs; L2 (F-corpus) is the multi-day remainder (launch-ready).

## Leg outcomes

| leg | status | of-record |
|---|---|---|
| **L1 — multi-tool real-tool arm** | ✅ DONE (strong) | `RESULT-pws-l1-evomaster.md`. EvoMaster 4 sites (all NOT_INTERPRETABLE/tool-not-runnable = a REACHABILITY barrier) + Schemathesis 2 sites (BY-CONSTRUCTION MISS = ORACLE blindness, incl. stateful) + RESTler considered-not-run. **Two real tools, two complementary fundamental reasons, both miss the masked class = a tool-CLASS property; MIST closes both (stimulus reaches + read-back sees).** Separate table, never merged. |
| **L3 — TT per-endpoint read-back re-capture** | ◐ MECHANISM-RESOLVED | `rater-sidecars-staging/L3-readback-bodies/`. The A3(ii) null-payload evidence-block is a sidecar-persistence gap, NOT structural: real read-back bodies captured (account/contacts/routes) + an end-to-end acting-record membership-extract demo. Full 9-case fault/control re-capture = the USER-witnessed seal step. |
| **L4 — Tracetest live** | ⊘ INSTALL-BLOCKED-DISCLOSED | `RESULT-pws-l4-tracetest.md`. The chart pins a dead Bitnami postgres image; the remaining javaagent instrumentation in a flap window crosses the ½-day stop rule. Surrogate cells STAND, labels unchanged (never relabeled/spun). |
| **L2 — F-corpus build** | ○ LAUNCH-READY, NOT-STARTED | `L2-fcorpus-launch-readiness.md`. The multi-day clean-room build of 7 faults (6/7 corrupted-class → in-corpus + MIST n_a out-of-scope-by-design; ≤1 lost F1; ≤2 new sites F8/F14). ADDITIVE to the 27-case validator-green corpus, never headline-load-bearing. Needs a sustained TT window + mvn/docker pipeline. |

## What PWS delivered for the reviewer attacks

- **#4/#5 (strawman / no real-tool / no execution-level comparison):** ANSWERED by L1 — a
  MULTI-TOOL real-tool comparison (EvoMaster + Schemathesis, both real, both run) showing
  the masked-2xx class is missed by the tool CLASS for two fundamental reasons, directly
  motivating MIST's stimulus + read-back deltas. (Cast stays closed/no-artifact — its
  execution-level comparison remains impossible, stated openly.)
- **#1/#2 (size / injected-vs-natural positives):** L2 is the ready-to-launch answer
  (upstream-grounded faults from the FudanSELab industrial-fault survey, clean-room), the
  multi-day remainder.
- **#6 (κ power):** L3 shows the +9-rateable-units branch is executable (seal-time).

## End-state

All tenants scaled to 0 (TT pods terminating at write time); RAM freed. Sealed sets
untouched. EvoMaster JAR gitignored (LGPL-3.0, sha-pinned). Corpus stands at 27
validator-green cases (PWS added evidence + the real-tool arm, no case/label changes).
