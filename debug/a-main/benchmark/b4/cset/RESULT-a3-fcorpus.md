# RESULT — A3 F-corpus build-out (the SIZE axis) — RESULT OF RECORD

**Date:** 2026-07-17 · Status: PIPELINE PROVEN + 2 HIGHEST-VALUE CASES LIVE-VERIFIED;
5 corrupted implemented+built, per-fault B-m6 owed (additive MIST-n_a breadth). Under
the A-VENUE STRENGTHEN wave (empirical/benchmark main-track goal); ADDITIVE, never headline.

## Pipeline proven end-to-end
implementer clean-room (7 faults on `fcorpus-build`, flagship byte-untouched, conduct
diff-reviewed PASS) → docker image build (JDK-8 in-container, the implementer's JDK-21
compile overrides irrelevant to the image = B3 resolved) → kind-load + set-image (5 touched
services on `:fcorpus`) → TT revived (doubleWrite=false PUT ok) → toggle (admin-bearer) →
class-aware B-m6 → live-verified masked write.

## Live-verified (2 highest-value cases)
| case | class | site | B-m6 | MIST column |
|---|---|---|---|---|
| **F1** `TT-cancel-refund-asyncrefund-f1-001` | **LOST** | cancel-refund (occupied; mechanism-variant) | **PASS** — control refund 50→130 lands / fault 50→50 LOST, both ack {1,Success} | **flag** (MIST-in-scope; the 10th MIST-caught positive) |
| **F8** `TT-user-selection-corrupt-f8-001` | CORRUPTED-present | ts-user-service (**NEW DISTINCT SITE**) | **PASS** — control documentType=1 / fault=0 (wrong), both ack 2xx | **n_a** (out-of-scope-by-design; benchmark broader than the tool) |

⇒ Corpus 27 → **29, validator-green**. MIST column: **flag 10 (+F1) / no_flag 13 /
principled-n_a 6 (+F8)**. Integration chain regenerated (census/visibility/map/scoring/
release-staging 42 members).

## The paper value delivered
- **F1** adds a NEW MIST-catchable acked-but-lost positive via a DISTINCT mechanism
  (async refund sequencing) on the flagship site, upstream-grounded (FudanSELab survey) —
  strengthening the positive side with a genuinely different, real-world-described fault.
- **F8** is the honest "benchmark BROADER than the tool" datum: a new-site
  corrupted-present write that is a valid benchmark case but OUT of MIST's lost-only oracle
  scope (MIST correctly n_a) — the anti-self-serving evidence the reviewers value.

## Remaining (implemented + built + kind-loaded; per-fault B-m6 owed — additive MIST-n_a breadth)
F14 (basic-service second-class price, NEW SITE, corrupted — needs the full trip-query
booking flow), F10 (contacts, occupied, corrupted), F11/F13 (cancel, occupied, corrupted),
F20 (order status, occupied, corrupted). All on `fcorpus-build`, images built + loaded;
each is CORRUPTED-present ⇒ MIST n_a; they add case-run DEPTH + F14 a 2nd new site. Owed a
per-fault B-m6 + case-authoring pass (a future short TT window; the pipeline is proven).

## End state
TT scaled to 0 at window close. Fork `fcorpus-build` branch holds the 7 implementations
(MIST-trainticket untouched). Sealed sets untouched.
