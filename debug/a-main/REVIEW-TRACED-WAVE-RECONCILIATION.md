# Traced-capture wave plan — 3-cold-review reconciliation (2026-07-10)

Reviews of `c2c3/traced-capture-wave-plan.md` (rev 1, commit 43fb289): **A feasibility / B evidential
soundness / C scope-value — ALL GO-WITH-CHANGES.** Every fix is plan-text; none is a redesign. The plan
is amended to **rev 2** in place; execution may start only from rev 2.

## Convergence table (≥2 reviewers)

| # | Finding | By | Disposition (rev-2 pin) |
|---|---|---|---|
| T1 | "see D3" dangles; traced adminroute control is NOT optional — an absence assertion is unfalsifiable without a same-deploy positive control | B-B4, C-B2 | Delete D3; **all four controls (#2/#4/#6/#8) are REQUIRED traced re-captures**; #6 stays on base 1.0.0 (digest binding preserved; flag-path code-identical) |
| T2 | Amending old sidecars = mixed-run Frankenstein artifacts (violates R16 precedent) | B-M2, C-B3 | **Traced capture = the case's capture-of-record**: complete NEW sidecar per leg; ALL 7 oracle columns re-recorded from that single run; old sidecars stay as superseded history; if a re-run diverges on any non-trace cell, the NEW measurement stands + divergence disclosed (never keep the better-looking run) |
| T3 | RAM: the "contingency" is the PRESENT state (581 MB free, swap active, consign-price CrashLoopBackOff ×166) | A-F1, C-M2 | Sub-batching = DEFAULT; rollouts strictly one-at-a-time; **scale-0 → single-patch → scale-1 (no surge)**; de-instrument batch 1 before batch 2; scale ts-consign-price-service to 0 for the wave (restore after); `free` + restart check between steps; node-level OOM at canary ⇒ pre-registered stop |
| T4 | Budget/stop discipline | C-M1, (A bottom line) | **Budget 2–3 days.** Stop rule: if batch 1 + scoring exceed 2 days, or a canary-class failure recurs on a second service, SHIP BATCH 1 ALONE; batch 2 folds into checklist 2.5.1 with a freeze-amendment disclosure that the comparator-favoring breadth cells remain pre-registered |
| T5 | G1 precedent uncited; agent version must be pinned to a known-good line | C-M3, A-F4 | Cite G1 (`gate1-result.md:168`, `gate1-smoke-result.md:39`: JTO+OTel agent ran full paired runs on this exact stack) → boot risk = "verify, don't fear"; pin the **1.x line (1.33.z final)** + sha256 + **the 1.x semconv attribute names** (`http.target`/`http.method`/`http.status_code`) next to the version pin (scorer breaks silently if the line changes); note base images = OpenJDK 8u111 (canary = contacts on 8u111 exercises the worst case first) |

## Blocking singles

| # | Finding | By | Disposition |
|---|---|---|---|
| T6 | **Presence semantics + span GRANULARITY unpinned — and the OTel agent instruments JDBC/Mongo BY DEFAULT, so the published trace pair itself shows the lost write's absence at DB-span granularity** ("trace-invisible-by-construction" is granularity-contingent; undisclosed = the presence arm looks like a strawman, provable from our own artifacts) | B-B1 | PIN: (i) `tracetest_presence` = **existence-only** assertion at **cross-service HTTP-span granularity** (selector = the `target.dependency` service's server span — what a Tracetest user names from the service map); the error/status axis belongs exclusively to `naive_span_error`; (ii) agent runs with **DEFAULT instrumentation — no `OTEL_INSTRUMENTATION_*_ENABLED=false` suppression** + an attestation recorded in the sidecar; (iii) **mandatory measured disclosure** per fabricated-ack case + in the N-vs-0 row: whether the absence IS visible at DB-span granularity and that a statement-level presence assertion would catch it (disclosed, this STRENGTHENS the read-back story: black-box equivalent of white-box statement assertions); (iv) `trace_visibility` follows the pinned granularity with the DB-layer sensitivity note |
| T7 | createaccount presence cell decidable TODAY, not "from the actual trace" | B-B1c | PIN NOW: `tracetest_presence = not_applicable` on both createaccount legs (in-process persistence — no cross-service span to name at the pinned granularity). Consequence pre-registered: the presence column's run-and-missed evidence is **N=1 (cancel)**, not N=2; createaccount contributes via naive (+ mist_trace_shape if run) |
| T8 | Scoring script + selector table must be COMMITTED BEFORE the first real capture | B-B2, C-m3/m4 | §6 gains step 1.5: commit the script + frozen per-case selector table (entry-span selector; presence selectors — cancel: ts-inside-payment server span for POST /drawback; adminroute: ts-route-service server span; adminbasic: ts-contacts-service server span; createaccount: not_applicable; error-span def = exported ERROR status, mechanical, scoped to the 7 instrumented services; trace selection = entry service+operation+WSL-clock window, **exactly-one-match else ERROR**) BEFORE captures; script debugged ONLY on canary/throwaway traces; record selector authoring minutes (freeze `authoring_cost` datum); disclosure line: the presence column is scored by the committed script implementing presence SEMANTICS on exported traces — the Tracetest product does not run in this wave |
| T9 | `mist_trace_shape` disposition leaves the N-vs-0 row UNEARNED as the freeze words it ("the trace oracles ran and missed"); `not_applicable` loses its R2 no-input justification once traced | B-B3 | **Branch A attempted first, per leg, with existing vehicles only (no tool-code changes)**: a bounded mist-cli run at the pinned commit 7d69de9 with `jaeger.base.url` bound against the traced deploy where the demo corpus covers the path (adminroute: covered — 2527 refs; cancel/createaccount: only if an existing harness binds trace-shape without code changes). Any leg where no existing vehicle binds → **Branch B for that leg**: disclosed freeze §6 amendment scoping the row's "trace oracles" to the comparator columns, cell carries a prose note "traced-but-not-run; deferred to <named wave>" (distinct from R2's no-input `not_applicable`). One-line symmetry pre-empt: `mist_readback` is fillable without running MIST because the typed readback contract (R4) makes it construction-deterministic; `mist_trace_shape` has no typed contract — exactly why hand-derivation is banned |
| T10 | Relationship to checklist 2.5.1/2.15(b) unstated; restore-vs-converge ambiguous | C-B1 | **Pilot framing pinned**: this wave partially discharges 2.5.1 (TT write-path javaagents) as a de-risking pilot; instrumentation TORN DOWN after (preserves the DoD-passed baseline); the runbook + pinned agent version + scoring script are the deliverables 2.5.1/2.5.2 consume; checklist 2.5.1 row updated with a pointer; **the TT row of 2.5.4's trace-coverage table produced from this wave's actual traces** (nearly free); the 2.15(b) lean-traced convergence remains a SEPARATE later decision |

## Majors folded

- **Jaeger v2 reality** (A-F2/F3, C-m1): export URL = **`/jaeger/api/traces`** (v2.14 `base_path`), `start`/`end` = epoch **microseconds**, explicit `limit`; badger sits on an **emptyDir** → **export + verify non-empty IMMEDIATELY after each leg**, never batch to the end; zipkin-9411 fallback confirmed real (receiver active); OTLP compat confirmed.
- **Unrelated error spans** (B-M1): naive stays mechanical (no exclusions ever); canary gate = each path's probe trace must be error-span-free before real captures (remediation only for deploy-health causes, disclosed); a real capture showing a background error is recorded AS MEASURED + diagnosed in notes; captures on a quiescent cluster (no concurrent traffic), disclosed. If RestTemplate context propagation is broken: window correlation may support span-PRESENT verdicts only — **absence-based `flag` cells are NOT claimable from window correlation**; they stay unfilled + disclosed.
- **Controls' `trace_visibility`** (B-M3): pinned NOW — a control carries its positive twin's instrumented-condition axis value + the fixed note "the value describes the PAIR's visibility regime; the control leg has no fault to be visible".
- **Pre/post health gates** (C-M4): pre-wave `kubectl get deploy -o yaml` snapshot of the 7 deployments (mechanical restore reference); pre-wave free-RAM + pod-health check with a numeric proceed threshold (≥400 MB free after consign-price scale-0, else stop); post-restore = demo-DoD key-path smoke (login + adminroute GET + cancel-path probe), not just 200s.

## Minors folded (one line each)
WSL-clock t0/t1 (skew ≈ 0 in-VM; id-in-URL cross-check exists only for the cancel pair — admin ids ride in POST bodies) [A-F5]. One `kubectl patch` per deployment (volume+mount+env in one change), `docker exec mkdir -p /otel` before `docker cp`, `hostPath.type: File` [A-F6]. Grep pod logs for `OutOfMemoryError` + agent export failures at every probe-first step (restart-watching misses degraded JVMs) [A-F7]. Canary (contacts) de-instrumented after canary, re-instrumented in batch 2 — never carried through batch 1 [A-F8]. Fallback bake-in rebuild ⇒ new digest ⇒ label re-version + disclosure [B-m1]. Record the FULL `OTEL_*`/JTO env set + no-suppression attestation in each sidecar [B-m2]. Mesh-state precondition: no residual EnvoyFilter/VS fault config on inside-payment; scorer scoped to the 7 services defuses any Envoy spans [B-m3]. Pointer convention: positive case gets `fault_trace` + `control_trace` (twin's export); control case gets `control_trace` [B-m4]. Re-verify the ack-text tells on the new sidecars [B-m5]. Row-N wording pinned: "2 fabricated-ack positives, 2 defect sites, 1 service; presence run-and-missed N=1 (cancel)" [B-m6]. Optional BOUNDED rider: 30–60 min clean-traffic normal-corpus grab while traced (pre-feeds 2.5.6/2.5.7; skip ⇒ note the config-drift caveat) [C-m6]. Fork 1.0.5 images verified already kind-loaded — no builds needed [A verified, C-m5].

## Verdict
GO once rev 2 lands (this document + the amended plan). Value calibration accepted from C: this wave
moves no accept/reject calculus by itself (S3 + second SUT remain the binding constraints, both
user-gated); it retires the pilot's most visible internal IOU, de-risks 2.5, and is correctly-sequenced
TT-up work — bounded at 2–3 days by T4's stop rule.
